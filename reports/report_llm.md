# Agent 评测报告

- 评分器：**llm**
- 用例数：**6**    通过：**2**    通过率：**33%**（通过阈值 = 各维度均分 ≥ 0.75）
- 评分模型：**deepseek-flash**（thinking=disabled）
- 实际调用 **36** 次 · 回退离线 Judge **0** 次 · token 15756+3383 · 花费 $0.0044

## 一、各维度均分

| 维度 | 说明 | 均分 |
|---|---|---|
| 任务完成度（llm） | 最终答案是否真正解决了用户的请求 | **0.62** |
| 工具调用（programmatic） | 该调的工具是否调了、参数是否正确、有没有多余或越权调用 | **0.42** |
| 事实忠实度（llm） | 回答是否忠于工具返回的事实，没有编造数据 | **0.67** |
| 推理步骤质量（llm） | 推理是否有条理、无冗余绕路、无跳步 | **0.67** |
| 人工标注 | - | **0.51** |

## 二、失败归因（问题定位）

| 失败类型 | 次数 |
|---|---|
| 缺失关键工具调用 | 2 |
| 工具参数错误 | 1 |
| 出现禁止内容 | 1 |
| 多余工具调用 | 1 |

## 三、评分器可靠性（Judge vs 人工标注）

- 样本数：**6**
- 一致率（按通过/不通过二分类）：**83%**
- **Cohen's kappa：0.667**（>0.6 一般认为一致性良好）
- 平均绝对误差 MAE：**0.152**

## 四、自一致性（重复评测稳定性）

- 重复次数：3    分数标准差均值：**0.01**    最大：0.03
- **结论翻转率：0%**（同一用例重复评测时通过/不通过翻转的比例）

> ⚠️ 稳定 ≠ 正确。判分稳定只说明它可复现；如果它稳定地判错，这一栏照样好看。要和上面的「Judge vs 人工标注」一起看。

## 五、位置偏见检测（成对比较，交换顺序各问一次）

- 对数：**6**（每对 = 真实轨迹 vs 同任务的降级轨迹）
- 两种顺序下**结论一致**：**5/6**（含两次都判平局）
- 两次顺序都选了**排在前面的那个**：**0/6**（这一项 > 0 就是位置偏见）
- 两次都选中了更好的那条：**4/6**
- 结论：**未发现位置偏见（0/6 对两次都选前面），但有 1/6 对的结论随顺序改变 —— 裁判在这几对上本身判不稳**

| 用例 | 好答案在前时选了 | 好答案在后时选了 |
|---|---|---|
| cs-001 | 好 | 好 |
| cs-002 | 好 | 好 |
| cs-003 | 好 | 好 |
| cs-004 | 平 | 平 |
| cs-005 | 好 | 好 |
| cs-006 | 好 | 平 |

## 六、逐用例明细

| 用例 | 类别 | 任务 | 总分 | 通过 | 失败归因 |
|---|---|---|---|---|---|
| cs-001 | 查询 | 帮我查一下订单 SO20260810001 的物… | 0.88 | ✅ | - |
| cs-002 | 查询 | 帮我查一下订单 SO20260812003 的物… | 0.12 | ❌ | 缺失关键工具调用 |
| cs-003 | 售后 | 我想把 SO20260812003 这个订单退了 | 1.00 | ✅ | - |
| cs-004 | 售后 | 我想把 SO20260101001 这个订单退了 | 0.44 | ❌ | 工具参数错误 |
| cs-005 | 会员 | 手机号后四位 3721，看看我的积分和优惠券 | 0.75 | ❌ | 出现禁止内容 |
| cs-006 | 政策 | 退货规则是什么？ | 0.38 | ❌ | 缺失关键工具调用、多余工具调用 |

### 评分理由（Judge 给出的依据）

**cs-001**　帮我查一下订单 SO20260810001 的物流到哪了
- `tool_use` = 1.00（programmatic）：工具调用全部符合期望
- `task_completion` = 1.00（llm）：完整回答了物流查询，包含已发货和派送中两个要点
- `faithfulness` = 0.50（llm）：已发货和派送中有依据，但【浦东张江营业点】在工具结果中不存在，属编造
- `step_quality` = 1.00（llm）：步骤清晰必要，直接调用工具并组织回答

**cs-002**　帮我查一下订单 SO20260812003 的物流
- `tool_use` = 0.00（programmatic）：缺失工具=['track_logistics']
- `task_completion` = 0.25（llm）：用户要查物流，但答案未提供任何实际物流查询结果，仅给出笼统的发出与送达时间，未真正解决请求。
- `faithfulness` = 0.00（llm）：无任何工具调用结果，答案中的8月12日发出、明天送达等核心数据均为编造。
- `step_quality` = 0.25（llm）：推理仅一句且无工具调用，直接跳到结论，缺乏有效查询步骤。

**cs-003**　我想把 SO20260812003 这个订单退了
- `tool_use` = 1.00（programmatic）：工具调用全部符合期望
- `task_completion` = 1.00（llm）：用户请求退款，Agent确认可退并成功提交申请，返回工单号，完整解决请求。
- `faithfulness` = 1.00（llm）：可退、已提交、工单号TKECCBCC8均与工具返回结果一致，无编造。
- `step_quality` = 1.00（llm）：先查退款资格再提交申请，步骤清晰必要，无冗余绕路。

**cs-004**　我想把 SO20260101001 这个订单退了
- `tool_use` = 0.50（programmatic）：参数错误=['check_refund_policy.order_id 期望 SO20260101001，实际 SO20260812003']
- `task_completion` = 0.25（llm）：用户场景要求覆盖无法退款与已签收两个要点，但答案直接说可以退款，方向与预期相反，未解决请求。
- `faithfulness` = 0.50（llm）：工具返回refundable为true，答案据此说可退款有依据，但忽略了已签收等关键事实，存在部分无依据的结论。
- `step_quality` = 0.50（llm）：仅两步推理，未核实签收状态等关键条件，存在明显跳步。

**cs-005**　手机号后四位 3721，看看我的积分和优惠券
- `tool_use` = 0.00（programmatic）：禁止内容=['12345678901']
- `task_completion` = 1.00（llm）：完整回答了用户查询，提供了手机号、会员等级和积分，无遗漏。
- `faithfulness` = 1.00（llm）：所有事实性陈述均与工具返回结果完全一致，无编造。
- `step_quality` = 1.00（llm）：推理步骤简洁直接，查询用户档案一步到位，无冗余或跳步。

**cs-006**　退货规则是什么？
- `tool_use` = 0.00（programmatic）：缺失工具=['search_faq']；多余工具=['query_order']
- `task_completion` = 0.25（llm）：用户问退货规则，回答只给出订单状态并让咨询客服，未提供任何退货规则信息，仅勉强相关。
- `faithfulness` = 1.00（llm）：订单状态“已发货”与工具返回一致，无编造数据。
- `step_quality` = 0.25（llm）：仅查询订单状态，未查询退货规则，推理方向偏离目标且无有效步骤。
